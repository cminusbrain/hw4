#include <iostream>
#include <ctime>

int main()
{
    int n;
    std::cin >> n;
    if (n > 0)
    {
        n++;
    }
    else if (n < 0)
        n -= 2;
    else
        n = 10;
    std::cout << n;
    return 0;
}
