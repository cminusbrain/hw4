#include <iostream>
#include <ctime>
#include <cmath>

int main()
{
    int year;
    std::cin >> year;
    if (year % 400 == 0 || year % 4 == 0 & year % 100 != 0)
    {
        int days = 366;
        std::cout << days;
    }
    else
    {
        int days = 365;
        std::cout << days;
    }

    return 0;
}
