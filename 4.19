#include <iostream>
#include <ctime>

int main()
{
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
    if (a != b && a != c)
    {
        std::cout << 1;
    }
    if (a != b && b != c)
    {
        std::cout << 2;
    }
    if (c != a && c != b)
    {
        std::cout << 3;
    }
    else 
        std::cout << 4;
    return 0;
}
