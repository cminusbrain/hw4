#include <iostream>
#include <ctime>

int main()
{
    int a, b, c;
    std::cin >> a >> b >> c;
    if (a < b && a < c)
    {
        std::cout << b + c;
    }
    else if (b < c)
        std::cout << a + c;
    else 
        std::cout << a + b;
    return 0;
}
