#include <iostream>
#include <ctime>
#include <cmath>

int main()
{
    int x, y;
    std::cin >> x >> y;
    if (x && y)
    {
        std::cout << 0;
    }
    else if (x)
    {
        std::cout << 2;
    }
    else if (y)
    {
        std::cout << 1;
    }
    else 
        std::cout << 3;
    return 0;
}
