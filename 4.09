#include <iostream>
#include <ctime>

int main()
{
    int a, b;
    std::cin >> a >> b;
    if (a > b)
    {
        std::cout << "A = " << a << " " << "B = " << b;
    }
    else
        std::cout << "A = " << b << " " << "B = " << a;
    return 0;
}
